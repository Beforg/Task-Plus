class Tarefa {
    id;
    nome;
    descricao;
    dataHora;
    concluido;
    constructor(id, nome, descricao, dataHora, concluido) {
        this.id = id;
        this.nome = nome;
        this.descricao = descricao;
        this.dataHora = dataHora;
        this.concluido = concluido;
    }
}
